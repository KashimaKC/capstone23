import { View, Text } from "react-native"

const SettingsPage = ( ) => {
    return (
        <View>
            <Text style={{color: 'black', marginTop: 25}}>Settings</Text>
        </View>
    )
}

export default SettingsPage